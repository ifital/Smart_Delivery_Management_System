<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/data/jpa
           https://www.springframework.org/schema/data/jpa/spring-jpa.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">


    <!-- Enable JPA repositories -->
    <jpa:repositories base-package="org.example.repository"/>

    <context:component-scan base-package="org.example"/>

    <!-- Load database properties -->
    <mvc:annotation-driven />

    <!-- DataSource -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource">
        <property name="driverClassName" value="org.postgresql.Driver"/>
        <property name="jdbcUrl" value="jdbc:postgresql://localhost:5432/Smart_Delivery_db"/>
        <property name="username" value="postgres"/>
        <property name="password" value="latifi-2001"/>
    </bean>

    <!-- EntityManagerFactory -->
    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan" value="org.example.model"/>
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.hbm2ddl.auto">update</prop>
            </props>
        </property>
    </bean>

    <!-- Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory"/>
    </bean>
    <!-- Déclaration des services -->
    <bean id="livreurService" class="org.example.service.LivreurService">
        <constructor-arg ref="livreurRepository"/>
    </bean>

    <bean id="colisService" class="org.example.service.ColisService">
        <constructor-arg ref="colisRepository"/>
        <constructor-arg ref="livreurRepository"/>
    </bean>

    <!-- Déclaration des controllers REST -->
    <bean id="colisController" class="org.example.controller.ColisController">
        <property name="colisService" ref="colisService"/>
    </bean>

    <bean id="livreurController" class="org.example.controller.LivreurController">
        <property name="livreurService" ref="livreurService"/>
    </bean>

</beans>
